server:
  port: 8081
  
spring:
  thymeleaf:
    prefix: classpath:templates/ 
    suffix: .html
    cache: false
    
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/localhost?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
#    password: qwer1234

mybatis:
  config-location: classpath:mybatis/config/mybatis-config.xml
  mapper-locations: classpath:mybatis/*/*.xml